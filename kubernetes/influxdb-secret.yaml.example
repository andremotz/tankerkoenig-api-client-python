# InfluxDB Secret Template
# Kopiere diese Datei zu influxdb-secret.yaml und fülle die Werte aus

apiVersion: v1
kind: Secret
metadata:
  name: influxdb-secrets
  namespace: default  # Anpassen falls nötig
type: Opaque
stringData:
  admin-password: "YOUR_ADMIN_PASSWORD_HERE"  # InfluxDB Admin-Passwort
  admin-token: "YOUR_ADMIN_TOKEN_HERE"  # InfluxDB Admin-Token (für API-Zugriff)

